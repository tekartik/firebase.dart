# Firestore rest Setup

In `pubspec.yaml`:

```yaml
dependencies:
  tekartik_firebase_rest:
    git:
      url: https://github.com/tekartik/firebase.dart
      path: firebase_rest
      ref: dart3a
    version: '>=0.6.2'
```